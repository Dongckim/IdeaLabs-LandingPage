{% extends "base.njk" %}

{% block content %}

    <section class="site-contact">
      <div class="container">
        <div class="row">
          <div class="col-lg-5">
            <h1 class="site-contact-title">
              {{title}}<span>{{subTitle}}</span>
            </h1>
          </div>
          <div class="col-lg-6 ml-auto">
            <div class="site-contact-form">
              <h4>{{formTitle}}</h4>
                <button type="submit" class="btn btn-primary" formtarget="_blank">
                  <span class="btn-area">
                    <a href="https://oqker7lu.forms.app/lambda5th-recruiting"/>
                    <span data-text="{{btnText}}">
                      {{btnText}}
                    </span>
                  </span>
                </button>
              </div>
              </div>
              <p class="contact-form-generator mt-3">
                <a>Contact us if you want to join!</a>
              </p>
            </div>
          </div>
        </div>
      </div>
    </section>
  
{% endblock %}
{% block workTogether %}{% endblock %}
